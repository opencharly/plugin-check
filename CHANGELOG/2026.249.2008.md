# 2026.249.2008 — refactor(check): migrate to the uniform ordered member tree (spec #103 + sdk #221)

## Summary

Migrates plugin-check to the ONE ordered member tree (spec #103, Deploy.Member []spec.Member{Name, Position, Node}) and the sdk #221 unified memberDisc classification. The spec v0.2026249.1948 dead surface (the dual Children/Members maps, SortedMemberKeys, SortedNestedKeys, ValidateDeploymentChildren, HasChildren, Inside) is fully consumed away:

- **Pins**: spec v0.2026249.910 → v0.2026249.1948, sdk v0.2026249.856 → v0.2026249.1959 (go.mod/go.sum, go mod tidy).
- **bed_persist.go**: member persist iterates root.DeployLevelMembers() (authored order), reading m.Name/*m.Node — the former spec.SortedMemberKeys(root.Members) map walk is gone.
- **bed_session.go**: bedMemberDescriptors/bedLocalChildKeys take []*spec.Member (DeployLevelMembers/InSubstrateMembers, authored order replaces sorted map keys); the descriptor now derives CheckLiveRefs via fleet.BedCheckLiveRefs(bed, &node) (new node-walking signature), ChildKeys via the new memberNames(InSubstrateMembers()), Members via bedMemberDescriptors(DeployLevelMembers()).
- **cmd_helpers.go** (resolveNestedNode) + **venue.go** (resolveDeployNodeByPath): dotted-path descent now walks MemberByName + InSubstrate per segment — never a dual-map index.
- **instrument.go**: member-venue instrument resolution parses the serialized tree wire shape [{name, position, node}] (one member array replaces the former "peer"/"nested" wire maps; the sort.Strings key-order shim is deleted — the tree preserves authored order).
- **live_gather.go**: group-bed header counts entry.DeployLevelMembers().
- **Tests migrated to the tree**: live_gather_test.go, score_live_test.go, venue_test.go fixtures build Member slices with Position: spec.PositionInSubstrate; instrument_test.go's serialized-node fixture carries the member array.
- Not changed (correctly maps): d.Members in bed_run.go is spec.CheckBedReply.Members (the descriptor slice, not the dual map); map[string]spec.FleetNode root maps and rp.Deploy (spec: map[string]*Deploy) stay maps — the tree replaces the member EDGES only.

## How tested

`go build ./...` → BUILD_OK (exit 0); `go vet ./...` → VET_OK (exit 0); `go test ./... -count=1` → TEST_EXIT=0:
```
TEST_EXIT=0
ok  	github.com/opencharly/plugin-check/candy/plugin-check	1.112s
?   	github.com/opencharly/plugin-check/candy/plugin-check/cmd/serve	[no test files]
```
gofmt -l . → clean. Executed sweep (B14(d)) — real git grep outputs, all ZERO matches on the final tree:
```
$ git grep -nE "SortedMemberKeys" -- "*.go"          → (zero matches)
$ git grep -nE "SortedNestedKeys" -- "*.go"          → (zero matches)
$ git grep -nE "ValidateDeploymentChildren" -- "*.go" → (zero matches)
$ git grep -nE "HasChildren" -- "*.go"               → (zero matches)
$ git grep -nE "\.Inside\(" -- "*.go"                → (zero matches)
$ git grep -nE "\.Children" -- "*.go"                → (zero matches)
$ git grep -nE "Children:" -- "*.go"                 → (zero matches)
$ git grep -nE "Members\[" -- "*.go"                 → (zero matches)
$ git grep -nF '"peer"' -- "*.go"                    → (zero matches)
$ git grep -nF '"nested"' -- "*.go"                  → (zero matches)
```
The only map[string]*spec.FleetNode remaining is derefDeployTree over rp.Deploy — the FLEET ROOT map, which spec v0.2026249.1948 itself keeps as map[string]*Deploy (cue_types_gen.go:2020); the member tree replaced the member edges, not the roots map.

## Rulebook compliance

- **R0 (skills first)**: git-workflow + go SKILL.md loaded before the first tool call; repo has no local AGENTS.md/CLAUDE.md (verified ENOENT).
- **R1 (RCA)**: the two test failures after the first build (instrument entries 2≠3, dispatched ops 4≠6) were RCA'd to the test fixture still carrying the DEAD serialized wire shape ("peer" map) — the production reader had correctly moved to the member array; fixed the fixture, not the reader.
- **R2**: n/a (no scope boundary crossed).
- **R3 (no duplication)**: position classification consumed via spec's derived helpers (Alongside/InSubstrate/MemberByName/DeployLevelMembers/InSubstrateMembers/HasMembers); no dual-map or position logic re-implemented locally.
- **R4 (no workarounds)**: no sleeps, retries, pins-by-hand (go get + go mod tidy), or CI fixes.
- **R5 (delete legacy completely)**: the sorted-map-key shims (spec.SortedMemberKeys / fleet.SortedNestedKeys call sites and the sort.Strings key-order shim in instrument.go) are deleted in the same PR; the legacy wire-shape reader is gone.
- **R6 (git safety)**: no force-push, no hook bypass, no direct push to main; git status reviewed before commit.
- **R7 (prove the gate)**: build + vet + go test -count=1 executed on the final committed tree; tails pasted above (this repo is a Go-only plugin — the umbrella task verify does not apply inside a submodule; the module's own gate is go build/vet/test).
- **R8**: n/a.
- **R9**: n/a (no secrets/credentials).
- **R10 (fresh disposable proof)**: gates re-run AFTER the commit from the committed tree (TEST_EXIT=0 above); no edited-state proof.

## Change classification

- **Change class**: dependency cutover + internal Go refactor (API surface of the plugin unchanged; the bed-runner member behavior is order-identical to authored order — a strict improvement over the former sorted map keys).
- **Verification gate**: go build / go vet / go test -count=1 on the final committed tree (evidence above).
- **Attribution tier**: fully tested and validated (all three gates green on the final tree; the migrated paths are exercised by the migrated unit tests).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*