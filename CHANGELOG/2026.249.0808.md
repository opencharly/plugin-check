# 2026.249.0808 — fix(check): allowlist the cachyos binutils + libtool local-newer warnings

## Summary

Allowlist the cachyos binutils + libtool "local newer than repo" warnings (RCA 2026-09-06). The R10 bed run (check-instrument-cachyos-vm 2026.249.0739) surfaced them as UN-allowlisted — the SAME upstream skew the zstd/pacman-contrib entries document (the published CachyOS image installs newer builds than its repos carry; no pin charly can choose removes it). Per the documented one-package-per-entry pattern, with the match parens ESCAPED (the exact fix the zstd entry uses — my first pass used unescaped capture-group parens, caught here because the gating test fails without a working match).

## How tested — LIVE, changed code path executed

The changed code path ran live in the fresh-rebuild cachyos instrument bed (charly binary rebuilt against this branch):

```
$ charly check run check-instrument-cachyos-vm    # binary rebuilt with THIS PR's entries
run 2026.249.0801 — PASS (steps=9), exit=0, total 107s
  deploy-add 48s ok (warnings 0, allowlisted 11)
  update 45s ok (warnings 0, allowlisted 15)
  diagnostics: errors 0, warnings 0, allowlisted 31

summary.yml allowlist_used (verbatim ids):
  - cachyos-binutils-local-newer-than-repo   (suppressed 4 — the lines at 0739 deploy-add 29-30)
  - cachyos-libtool-local-newer-than-repo    (suppressed 4 — the lines at 0739 update 46-47)
  - cachyos-zstd-local-newer-than-repo       (pre-existing pattern, still applied)
$ go test -run 'TestCachyosLocalNewerAllowances' -v .
--- PASS: TestCachyosLocalNewerAllowances (0.00s)     # FAILS with any entry removed
$ go test ./...
ok  	github.com/opencharly/plugin-check/candy/plugin-check	5.109s
```

The 8 warnings run 0739 reported as UN-allowlisted are gone: binutils×2 + libtool×2 now discharge under the two new entries (allowlist_used shows ids cachyos-binutils-local-newer-than-repo and cachyos-libtool-local-newer-than-repo in the live run's summary), zstd stays under the pre-existing entry. The test gates all three classes — removing any entry fails the test.

## Rulebook compliance

- R1: the warnings RCA'd above (CachyOS upstream skew — "no pin charly can choose removes it").
- R3: one allowlist entry per package (the documented pattern; the Why text explains the exemption, printed into summary.yml).
- R4: no workarounds — the entries carry reviewed why-text; the fix (escaped parens) matches the zstd entry exactly.
- R10: the live bed re-run above (fresh-rebuild binary) shows the changed regexes executed against the real 0739 warning lines, at ZERO un-allowlisted warnings.

## Change classification

Class: diagnostics policy (allowlist entries + gating test). Gate: live fresh-rebuild bed run (pasted above) + unit tests. Attribution: fully tested and validated (changed code path executed live at ZERO un-allowlisted warnings).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*