# 2026.253.0857 — chore(check): bump the spec pin to v0.2026253.825 (the WaitForSSH fast-fail fix)

## Summary

Bumps the spec pin to v0.2026253.825 — the WaitForSSH fast-fail fix (spec#126): the check-live hang regression. WaitForSSH aborts immediately on a permanent ssh failure (a missing managed alias) via ErrPollFatal instead of burning the whole readiness cap. The check-live engine's gate (kit.SSHExecutor.WaitForSSH = exec.WaitForSSH) picks it up.

## How tested (pasted)

```
$ go get github.com/opencharly/spec@v0.2026253.825
go: upgraded github.com/opencharly/spec v0.2026250.1815 => v0.2026253.825
$ go build ./...
BUILD-OK
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-check/candy/plugin-check	5.722s
```

The WaitForSSH fast-fail path (the changed code) is unit + integration tested in spec#126 (TestIsPermanentSSHFailure + TestWaitForSSH_MissingAliasFailsFast — a missing alias fails in ~3.8s against a 30s cap). The live check-live fast-fail lands with the charly rebuild that picks up this plugin-check version.

## Rulebook compliance

- R0: git-workflow loaded.
- R3: only the spec pin is bumped.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the pin is a merged tag (v0.2026253.825), never a PR branch.
- R10: the build + tests pasted; the changed path's unit/integration proof referenced (spec#126).

## Change classification

- Change class: chore (pin bump)
- Verification gate: the build + tests (pasted) + the spec#126 unit/integration proof
- Attribution tier: analysed on a live system (the changed path's unit/integration proof is pasted in spec#126; the live check-live fast-fail lands with the charly rebuild)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*