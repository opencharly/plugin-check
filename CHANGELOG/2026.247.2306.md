# 2026.247.2306 — feat(bed): the update_gate: change-class switch for the R10 fresh-update gate

## Summary

`update_gate:` — a declarative change-class switch for the check bed's R10 fresh-update gate (spec #91, merged + tagged v0.2026247.2009). Each bed/plan may declare `update_gate: full` (today's destroy/recreate + full re-check), `restart-only` (VM stop/start + re-check on the same clone — for runtime-PR-injection software-class evals), or `skip`. Anchored/pod paths handled; 14 unit-test cases in update_gate_test.go.

## How tested

**Full path (zero regression of the unchanged default)** — targets `disposable: true` (the eval beds):
```
charly check run check-omarchy-pr-10147-vm: PASS (steps=9) — run 2026.247.2023, total_seconds 93
phases: vm-build 1 / vm-create 3 / deploy-add 17 / check-live 14 / update 30 / check-live-rebuild 12 / cleanup 4 — all ok:true
```
Extended full-path proof: the 16-lane batch (matrix-16fix.md, all lanes concluded PASS 94-201s).

**restart-only (the new code path, fresh-rebuild bed run)** — target `disposable: true`:
```
charly check run check-omarchy-ugate-restart: PASS (steps=10) — run 2026.247.2025, total_seconds 335
phases: vm-build / vm-create / deploy-add / check-live / gate-restart-stop / gate-restart-start / check-live-rebuild / cleanup — all ok:true
bed: plan/ugate-live/charly.yml — update_gate: restart-only (line 210)
```
The gate-restart-stop/start phases exist ONLY via the new code path. Go unit tests (14 cases) + `go vet` green.

## Rulebook compliance

- R0: skills honored (`/charly-check:check` beds-and-r10 + the plugin skill); R1: the A2/B12 evidence gap RCA'd; R2: the live R10 in-tree + anchored to the named batches; R3: one canonical gate switch (restart reuses the existing stop/start + check-live); R4: no workarounds — the evidence is the real bed run; R5: n/a (no legacy cut here); R6: git-safe; R7: the unit suite + live runs pasted; R8: the spec pin bump consistent across go.mod/go.sum (security screen PASS); R9: the dev-worktree rebuild verified (`go version -m` on the tested binary shows the local-sdk devel provenance); R10: both evidence targets are `disposable: true` (runtime proof on disposable-only targets).
- B14: the restart path's stop/start is serialized behind the same per-domain locks as full (no new concurrency hazard); B15: hard cutover — the new field replaces nothing (default `full` = today's behavior); B16: the capability lives in the candy's bed runner, no core/sdk changes (placement PASS); B17: only disposable targets run (both evidence beds marked `disposable: true`); B18: the switch plugs into the existing phase machinery, no new coupling.

Change classification: feature (bed runner change-class switch + spec pin).

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated — live restart-only + full bed runs pasted)*