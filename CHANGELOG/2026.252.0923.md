# 2026.252.0923 — refactor(check): the checkkit cutover (R3 one-home) - the sdk v0.2026252.834 leg

## Summary
The R3 one-home cutover: the plugin-local verb dispatch copies are deleted. The embedded `checkkit.VerbResolver` now carries the real `RunProvisionAct` leg (the merged sdk#245, tagged v0.2026252.834) and `checkkit.PlanGrammar` is the one grammar — the do:act state-provision verbs dispatch through the SDK's one home, no plugin-local copy. The runner config threads the deployment's `mcp_provide` declarations (venue-aware: VM/host venues carry them, container venues resolve from their OCI image label) so the FRESH per-call snapshot (`SnapshotCheckEnv` → `kr.MCPProvide()`) rides them.

## How tested (pasted)
```
go build ./...  → BUILD_OK
go vet ./...    → VET_OK
go test ./...   → ok  github.com/opencharly/plugin-check/candy/plugin-check  0.580s
```
**Fresh-rebuild disposable bed run (A2):** `charly -C box/fedora check run check-pod` (disposable: true, fresh rebuild) — the full R10 sequence, all steps PASS:
```
[image-build]        PASS after 3m3.98s  [warnings=1 allowlisted=1 cache=5/138]
[check-image]        PASS after 16.312s [allowlisted=1]
[deploy-add]         PASS after 3.242s  [allowlisted=1]
[config]             PASS after 4.126s  [allowlisted=1]
[start]              PASS after 2.939s  [allowlisted=1]
[bring-up-members]   PASS after 49ms
[check-live]         PASS after 9.076s  [allowlisted=1]
[feature-run]        PASS after 7.671s [allowlisted=1]
[update]             PASS after 14.206s [allowlisted=5]
[check-live-rebuild] PASS after 7.959s [allowlisted=1]
[cleanup-members]    PASS
summary.yml: ok: true, total_seconds: 264, errors: 0
```
The bed's check-live + feature-run steps exercise the plugin-check's verb dispatch — the embedded checkkit resolver executing against the fresh rebuild (the changed path). Summary: `box/fedora/.check/check-pod/2026.252.0914/summary.yml`.

The committed tests exercise the cutover: TestPluginSnapshotCheckEnv_CarriesVmMcpProvide (the VM venue's mcp_provide rides the snapshot; the container venue stays unpopulated — the OCI label path owns it) + the pre-existing runner tests against the embedded checkkit resolver.

## Rulebook compliance
- R1 (RCA every anomaly): the container-venue snapshot regression was RCA'd — the NestedExecutor's descriptor Kind is empty, the podman-exec jump kind is the tell; the threading is jump-aware.
- R3 (one home, no duplication): the verb dispatch + the grammar live in the SDK's checkkit; the plugin-local copies are deleted in this PR.
- R4 (no workarounds): no sleeps/retries; the venue-aware threading is the mechanism's own contract.
- R5 (delete legacy completely): verb_resolver.go + plan_grammar.go are deleted in the same PR as the cutover.
- R6 (git safety): PR-only, no force-push, no direct main.
- R7 (prove the gate): the pasted build/vet/test outputs + the fresh-rebuild bed run above.
- R10 (fresh disposable proof): the changed path (the embedded checkkit resolver) ran live on the disposable check-pod bed against a fresh rebuild — the pasted step matrix above.
- N/A: R2 (no umbrella-level change), R8/R9 (no harness/CI change).

## Change classification
Refactor (R3 one-home cutover); verification gate: the pasted outputs + the fresh-rebuild bed run + the committed tests + the validator; attribution: fully tested and validated (the changed path ran live on the disposable bed against a fresh rebuild, and the full gate passed).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*