# 2026.250.1843 — fix: emit the check-run candy source dirs on the CheckEnv wire snapshot (committed-APK anchoring)

## Summary

Pure dependency pin: `spec` `v0.2026250.709` → `v0.2026250.1815` in
`candy/plugin-check/go.mod` (+ go.sum).

Context: the sender half of the committed-APK anchoring fix —
`pluginSnapshotCheckEnv` threading `RunnerConfig.CandyDirs` onto the wire
`spec.CheckEnv` — **already lives in `main`** (PR #36, tag v2026.250.1816). It
references the `spec.CheckEnv.CandyDirs` field that spec PR #121 added at
`v0.2026250.1815`. The module's own go.mod still pins `spec v0.2026250.709`
(predates the field), so this PR's pin bump is the compile requirement that
makes the already-merged sender build. There is NO code change in this diff —
the code change is PR #36 (base); this PR only resolves its go.mod requirement.

## How tested

```
$ cd plugin-check/candy/plugin-check
$ go build ./... && go vet ./... && go test ./...
ok  	github.com/opencharly/plugin-check/candy/plugin-check	2.651s
?   	github.com/opencharly/plugin-check/candy/plugin-check/cmd/serve	[no test files]
```

(the sender code compiles with this pin; the end-to-end bed proof travels on the
paired charly PR of the same cutover — the receiver half unwraps
`env.CandyDirs` into the host carrier at `plugin_dispatch_reverse.go`.)

## Rulebook compliance

- R3: single CUE-sourced wire shape (`spec.CheckEnv`); pin bump only, no
  hand-mirrored type.
- B13: pins are released tags only (spec v0.2026250.1815 = spec PR #121).
- PR-only landing; append-only branch.

## Change classification

- Change class: dependency pin
- Verification gate: go build/vet/test on the final tree
- Attribution: fully tested and validated (module gates on the final tree)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*