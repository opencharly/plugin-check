# 2026.249.1058 — feat(check): thread the deploy's from_snapshot into the vm-build steps (Phase 3)

## Summary

The **from: name:tag threading** in the check-bed runner (Phase 3): the deploy's unified snapshot (split at load) flows into the bed descriptor (CheckBedReply.FromSnapshot + CheckBedMember.FromSnapshot); the vm-build step passes `--from-snapshot <tag>` when set (`vmBuildArgs` — the unit-gated seam); the steps build the entity as a clone of its own golden at that snapshot.

## How tested (LIVE, pasted)

The threading executed LIVE in the converted instrument bed's vm-build step (disposable: true target, fresh-rebuild binary):

```
$ charly check run check-instrument-cachyos-vm    # run 2026.249.1032
[vm-build] PASS — the step drove the from-snapshot build; vm-build.log:
  Building VM "check-vm-clone-base" (source.kind=clone)
  Wrote output/qcow2/check-vm-clone-base/disk.qcow2 (clone of check-vm-clone-base@golden)
$ go test -run TestVmBuildArgsThreadsFromSnapshot -v
--- PASS (the --from-snapshot flag threading — fails without it)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: pasted live output; zero warnings besides the documented dev-worktree notice.
- R2: same named batch (Phase 3); the drive (#24/#25), the split (#217), the schema (#99-#101).
- R3: one flow — the deploy's FromSnapshot rides the descriptor exactly like VMTemplate.
- R4: no workarounds.
- R5: additive.
- R10: the threading ran live (pasted) + the args seam is unit-gated.

## Change classification

Class: bed-runner plumbing (functional half). Gate: unit gate + the live run. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*