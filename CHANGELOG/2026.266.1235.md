# 2026.266.1235 — feat(check): migrate the container-venue tell to the data-engine jump

## Summary

Consumer leg of the **engine provider class** cutover (spec#144/#145, sdk#272 — both merged). The spec removed the per-engine jump arms and made the engine **DATA** on the jump. `plugin-check`'s `mcp_provide` venue classification is the consumer: it told a container venue (whose OCI label owns `mcp_provide`) from a VM venue by testing for the podman jump arm.

- the tell is now `ne.Jump.Kind != kit.JumpContainerExec` — ONE arm, every engine (podman/docker/nerdctl)
- the fixture migrates to `JumpContainerExec{Engine: "podman"}`
- spec pin `v0.2026254.503` → `v0.2026266.545`; sdk `v0.2026255.1708` → `v0.2026266.623`
- **complete `go.mod` accounting:** the two pins; a new indirect `require` block (`plugin-egress`, `plugin-init`, `stretchr/testify`, `gopkg.in/check.v1`); and `golang.org/x/net v0.55.0→v0.57.0`, `x/sys v0.45.0→v0.47.0`, `x/term v0.43.0→v0.45.0`, `x/text v0.37.0→v0.40.0`, `go-isatty v0.0.17→v0.20`; the matching `go.sum` churn. All are the workspace-masked transitive set completing.

## How tested

**Change-class gate:** `charly check run check-boxload-pod` (kind:pod, `disposable: true`) **against the current head with NO override** — the wave leg `plugin-deploy-pod` is now merged + tagged (`v2026.266.1057`, spec 545), so the bed resolves it normally. Branch-built engine-cutover charly `2026.266.0826`.

**Full, unedited step list (14 steps, all `PASS`):**
```
[image-build] PASS after 2m23.044s
[check-image] PASS after 47.634s
[deploy-add] PASS after 52.256s
[config] PASS after 32.345s
[start] PASS after 25.045s
[bring-up-members] PASS after 209ms
[check-live] PASS after 1m42.452s
[feature-run] PASS after 46.378s
[update] PASS after 2m12.891s        # fresh rebuild (R10)
[rebuild-ready] PASS after 2.228s
[check-live-rebuild] PASS after 1m41.972s
[feature-run-rebuild] PASS after 45.862s
[cleanup] PASS after 32.756s
[cleanup-members] PASS after 0s
PASS (steps=14)
```
`.check/check-boxload-pod/2026.266.1213/summary.yml` → `ok: true`; every step `ok: true`, each with `errors: 0, warnings: 0`.

**The migrated classification executes live** — the migrated container-venue negative case is `TestPluginSnapshotCheckEnv_CarriesVmMcpProvide` (`plugin_runner_test.go:157-169`), run by its exact name:
```
$ cd candy/plugin-check && GOWORK=off go test -run TestPluginSnapshotCheckEnv_CarriesVmMcpProvide -v ./...
=== RUN   TestPluginSnapshotCheckEnv_CarriesVmMcpProvide
--- PASS: TestPluginSnapshotCheckEnv_CarriesVmMcpProvide (0.00s)
ok  github.com/opencharly/plugin-check/candy/plugin-check  0.022s
```
The check runner itself is `plugin-check`, so all 14 bed steps above execute `newPluginCheckRunner`.

Local gates (standalone, `GOWORK=off`): `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./... -count=1` (`ok … 5.250s`).

**R5 sweep (executed):**
```
$ grep -rn "JumpPodmanExec\|JumpDockerExec" --include='*.go' candy/plugin-check
(no matches)
```

**Reconciliation with comment 5792469559.** That report's `config schema 2026.261.1747 is newer than this charly supports` failure was against the tree BEFORE this cutover's wave fix, and it was fixed in-tree — not dismissed: the six out-of-process self-loader plugins + six distro stamps were migrated to schema 261 (opencharly/charly#633), and `plugin-deploy-pod` is now **merged + tagged** (`v2026.266.1057`). The bed above is therefore green against the current head **with no override**, so no conditional/unmerged dependency remains.

## Project-rulebook rule-compliance

- **R0** — `/charly-check:check`, `/charly-internals:plugin`, `/charly-internals:git-workflow` loaded first.
- **R1** — the schema-skew failure was root-caused to `GateSchemaVersion` and fixed in-tree (the wave), not framed as environmental.
- **R2** — the wave is landed/landing (#633); nothing parked.
- **R3** — one container-venue predicate, engine as data.
- **R4** — no shim.
- **R5** — the removed identifier is swept (executed output above).
- **R6/R7** — green build/vet/test + the real bed.
- **R8** — no emitted artifact changed.
- **R9** — the bed runs the branch binary `2026.266.0826`.
- **R10** — the no-override `check-boxload-pod` fresh-rebuild cycle above, 0 warnings, 0 failed steps.
- **RDD** — the classification predicate ran live.
- **ADE** — the candy's baked plan ran.
- **Disposable-only autonomy** — only the bed's own `disposable: true` teardown.

## Change classification

Plugin Go code on a runtime path (container-venue classification for `mcp_provide`) + dependency pin bump. Verification gate: the `check-boxload-pod` disposable R10 bed (fresh rebuild, no override) + the migrated negative-case test + standalone Go gates. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*