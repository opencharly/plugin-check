# 2026.255.1851 — chore(check): drop two dead allowances — an errcheck exclusion that suppressed nothing + 4 stale go.sum lines

## Summary

A dead-weight audit of `opencharly/plugin-check` — every diagnostic allowlist entry, the store-bloat
scaffolding, and any other zero-functionality leftover — and removal of the two things the audit could
**prove** dead:

1. `candy/plugin-check/.golangci.yml` — the `(io.Closer).Close` errcheck exclusion. It suppresses
   **nothing**: with it deleted the linter still reports `0 issues.`. The fmt.Fprint trio is
   load-bearing (removing each yields 1 / 3 / 3 errcheck issues — pasted below) and stays.
2. `candy/plugin-check/go.sum` — **4 stale checksum lines** for superseded `opencharly/sdk`
   `v0.2026252.834` and `opencharly/spec` `v0.2026253.825` (left behind by the #40 checkkit cutover and
   the #41 spec bump). `go mod tidy -diff` on the pre-change tree prints exactly those four lines.

Everything else audited is **not** dead and is reported here with its evidence rather than deleted.

## 1. The diagnostic allowlist — 18/18 entries are LIVE, none removed

`bed_diagnostics.go`'s `diagnosticAllowlist` was checked two ways:

* **(a) literal grep** across every producer I can see (umbrella `plugin-*` candies, `charly/`, the
  candies' own `plan:` steps): no allowance literal is emitted by **in-tree** code — every entry names
  an **external** producer (pacman, mkinitcpio, grub-probe, systemd/RPM scriptlets, dnf, pip,
  update-rc.d, limine-entry-tool), which is the documented keep-reason.
* **(b) a scanner census** — a temporary, uncommitted test in this package ran the REAL
  `scanStepDiagnostics` + `allowlistUsage` over **7,222 retained step logs** (`**/.check/**/*.log`
  across the umbrella tree). It counts lines an entry actually **CLAIMS** (pattern + conditional
  recovery) — strictly stronger than a literal grep.

```
$ CHECK_CENSUS_ROOT=<umbrella> go test -count=1 -run TestAllowlistCensusTmp -v ./
LOG-FILES-SCANNED: 7222
ALLOWLIST|cachyos-zstd-local-newer-than-repo|LIVE|490
ALLOWLIST|cachyos-pacman-contrib-local-newer-than-repo|LIVE|431
ALLOWLIST|cachyos-binutils-local-newer-than-repo|LIVE|59
ALLOWLIST|cachyos-libtool-local-newer-than-repo|LIVE|59
ALLOWLIST|pacman-repo-serves-older-than-installed|LIVE|29
ALLOWLIST|pacman-needed-package-already-current|LIVE|1568
ALLOWLIST|pacman-mirror-retrieval-recovered|LIVE|15
ALLOWLIST|pip-resolver-conflict-recovered|NEVER-CLAIMED|0
ALLOWLIST|dnf-commandline-skipped-gpgcheck|LIVE|1
ALLOWLIST|update-rc-d-chroot-fallback|LIVE|3
ALLOWLIST|systemd-unit-file-daemon-reload|LIVE|15
ALLOWLIST|limine-esp-not-mounted-in-chroot|NEVER-CLAIMED|0
ALLOWLIST|mkinitcpio-chroot-autodetect-fallback|LIVE|41
ALLOWLIST|pacman-hook-failed-mkinitcpio-recovered|LIVE|11
ALLOWLIST|mkinitcpio-chroot-warnings|LIVE|191
ALLOWLIST|pacman-pacnew-config-notice|LIVE|41
ALLOWLIST|dev-worktree-binary-notice|LIVE|8134
ALLOWLIST|grub-probe-fuse-overlayfs-recovered|LIVE|1
```

The two `NEVER-CLAIMED` entries are **kept** — external producers, live surfaces, and no way to prove
a negative from log retention:

| entry | producer (how checked) | why kept |
|---|---|---|
| `pip-resolver-conflict-recovered` | pip's own resolver notice; its conditional recovery line `Successfully installed ` occurs **8×** in the retained corpus; pip ships nowhere in-tree | its surface `pod-jupyter-ml` (vllm pinned requirements) is still in the tree |
| `limine-esp-not-mounted-in-chroot` | `limine-entry-tool`'s alpm hook; its recovery line `Updated: /boot/limine.conf` occurs **22×** in the retained corpus | `omarchy-pacstrap-vm` + `check-omarchy-pacstrap-vm` are both still defined (`distro-omarchy/charly.yml:1496`, `:1554`); the bed simply has no retained run in this tree |

Also kept as the documented **generic-pattern** exception: `mkinitcpio-chroot-warnings` (one
alternation over a chroot-artifact family of four sentences).

## 2. Store-bloat scaffolding — nothing here is that sentence's private scaffolding

Greps over the whole repo (`podman store is bloated`, `store-bloat`, `hygiene nudge`, `charly#173`)
return only two prose citations and the CHANGELOG. There is **no** store-bloat allowlist entry (the one
proposed by #47 never landed) and no store-bloat-specific code.

The **ADVISORY tier is NOT that sentence's scaffolding** — it is a general, live protocol recognizer:

1. It is **explicitly authorized as general** by the maintainer's T4 sign-off on #48 (comment
   2026-09-12T09:33:43Z): *"What is authorised: a third diagnostic tier whose lines are counted and
   REPORTED (`advisories: N`, `advisories_fatal: false`) and are never fatal under any policy, **and the
   recognizer that classifies `notice:` / `advisory:`-prefixed lines into it**, ahead of the warning
   recognizer."*
2. The census shows the tier **claiming a second, independent producer's line** — the Arch `tar`
   package's install scriptlet, emitted during `pacman -Syu` in a build with nothing to do with podman
   storage:

```
$ rg --hidden -uu -n 'NOTICE: tar backup' charly/box/cachyos/.check/check-openclaw-desktop-pod/2026.246.1814/image-build.log
235:upgrading tar...
236:==> NOTICE: tar backup/restore scripts have been moved
# ... and the census, classifying it through the real scanner:
SHAPE|advisory|advisory-prefix||3      # 2x the tar NOTICE + 1x the store nudge
```

**This PR is the consumer-side half of the plugin-build removal, and it supersedes plugin-build#11's
relabel.** #11's premise — relabel the nudge to `notice:` so the R10 bar stops counting it — is moot
once the emitter is deleted: the nudge should not return at **any** severity. Deleting
`candy/plugin-build/store_bloat.go` plus its `drive.go` call site needs **no companion change here** —
that is the finding, and it is why this PR removes nothing on the advisory side.

Residue for **that** PR to sweep (named, not an unnamed parked finding): the two prose citations of the
then-deleted emitter — `bed_diagnostics.go:80`, `bed_advisory_tier_test.go:9/12/36`. They are accurate
**today** (plugin-build `main` still emits the line — verified via `git ls-tree origin/main
candy/plugin-build/`), so they are not dead weight now.

## 3. Other zero-functionality leftovers

**Removed (provable):**

| file:line (pre-change) | what | proving evidence |
|---|---|---|
| `candy/plugin-check/.golangci.yml:20` | errcheck `exclude-functions: - (io.Closer).Close` | delete the line → `golangci-lint run ./...` = `0 issues.`; every `Close` in the module is already explicit (`_ =`, returned, or a load-bearing per-site `//nolint:errcheck` at `stream.go:53`, `note.go:120`, `harness_loop.go:299/304/316`) |
| `candy/plugin-check/go.sum:67,68,71,72` | stale sums for sdk `v0.2026252.834` + spec `v0.2026253.825` | `go mod tidy -diff` on the pre-change tree prints exactly those 4 lines (pasted below); the live requires are sdk `v0.2026255.1708` / spec `v0.2026254.503` |

**Checked and found clean** (negative results, so the audit is not mistaken for a clean bill of health):
no dead build tags, no `testdata` directories, no commented-out code, no unused identifiers
(`unused`/`staticcheck`: 0 issues), no unused `go.mod` requires (`go mod tidy` changes **only**
`go.sum`), no test helper or exported symbol without a caller, and every Kong field is either read by
code or is a dispatched subcommand leaf.

**Found, deliberately NOT fixed (would loosen the gate — a T4 change, not a removal):**
`bed_diagnostics.go:343` `systemd-unit-file-daemon-reload` requires a marker prefix
(`==>|-->|>>>|***`) before `Warning: The unit file…`. The entry IS live — it claims the `>>>`-marked
form (15 lines, `distro-fedora/.check/check-fedora-vm/2026.236.1426/deploy-add.log`). But the SAME
producer also emits the **bare** form, which the entry does NOT claim:

```
$ rg --hidden -uu -c --no-heading '^\s*(==>|-->|>>>|\*\*\*) Warning: The unit file' -g '*.log' .   -> marked=15
$ rg --hidden -uu -c --no-heading '^Warning: The unit file' -g '*.log' .                            -> bare=140
  e.g. charly/box/omarchy/.check/check-charly-omarchy-vm/2026.244.1118/deploy-add.log
```

Those 140 lines count as un-allowlisted warnings (reported, never fatal — nothing is hidden).
Widening the pattern is a gate-loosening change, so it is reported for a maintainer decision rather
than folded into a dead-weight PR.

## Removals found in OTHER repos (found, not edited — owners named)

| file:line | what | owner |
|---|---|---|
| `plugin-build/candy/plugin-build/store_bloat.go:80` | the `notice: podman store is bloated …` emitter (the rule the maintainer ordered deleted) | `opencharly/plugin-build` — the in-flight removal lane |
| `plugin-build/candy/plugin-build/drive.go:79-81` | its call site (`Store-bloat hygiene nudge …`) | `opencharly/plugin-build` |
| `plugin-build/candy/plugin-build/store_bloat_test.go:46-59` | the tests pinned to that sentence and to the `notice: ` prefix | `opencharly/plugin-build` |

Nothing outside this repo was touched.

## How tested

All gates on the **final committed tree** (`candy/plugin-check/`):

```console
$ gofmt -l .
   (empty)
$ go build ./...
$ echo $?
0
$ go vet ./...
$ echo $?
0
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-check/candy/plugin-check	20.212s
?   	github.com/opencharly/plugin-check/candy/plugin-check/cmd/serve	[no test files]
$ echo $?
0
$ golangci-lint run ./...
0 issues.
$ echo $?
0
```

The exclusion-removal proof (same tree, each line re-added on its own):

```console
$ # remove `fmt.Fprint` from the exclude list:
note_cmd.go:44:12: Error return value of `fmt.Fprint` is not checked (errcheck)
1 issues: * errcheck: 1
$ # remove `fmt.Fprintf`:
3 issues: * errcheck: 3
$ # remove `fmt.Fprintln`:
3 issues: * errcheck: 3
$ # remove `(io.Closer).Close`:
0 issues.
```

The `go.sum` proof, run against the PRE-change file:

```console
$ git show HEAD:candy/plugin-check/go.sum > go.sum && go mod tidy -diff
@@ -64,12 +64,8 @@
-github.com/opencharly/sdk v0.2026252.834 h1:4jfD81Drw6cqUPnoH7dfg2Ru1aspSmQTpT46b9Zhd5s=
-github.com/opencharly/sdk v0.2026252.834/go.mod h1:bHkz9iCZVrpRQ1GjmsqnnOZYkk6bidLqzmikJivJbrU=
-github.com/opencharly/spec v0.2026253.825 h1:o1xD6C+Rf94B3M0VHoPX6WmXATCvTWWIFnmQFV2Uekg=
-github.com/opencharly/spec v0.2026253.825/go.mod h1:yaB8ABSTiJClK1xq/zCtWizcqfKKOhnuKcgGrHwxmW8=
```

**Change class:** tooling config (`.golangci.yml`) + module metadata (`go.sum`). **No Go source and no
runtime path changed**; the candy manifest (`charly.yml`) and the plugin providers are untouched, so the
R10 bed roster is not the change-class gate here — the changed artifact is the linter config itself, and
it ran live (`golangci-lint run ./...` with the edited config = 0 issues). Stated explicitly so the
reviewer can disagree with the classification rather than discover it.

**A failure surfaced during verification, routed to its NAMED batch (R1/R2 — not parked).** Running the
repo's own CI step locally (`charly box validate`) with the *local dev* charly (`2026.248.2159`) exits 1:

```
$ charly box validate
charly: error: command "validate": validation error: /tmp/deadweight-pc/charly.yml: schema 2026.248.1030 is required (found "2026.232.0520"). Run: charly migrate
```

**RCA.** The root manifest is stamped `2026.232.0520`; the dev build's schema floor has since risen to
`2026.248.1030`. It is NOT a defect of this repo under its own contract: the candy CI gate runs the SAME
step with the PINNED charly (`deploy.yml`: `v2026.238.1242`) and is GREEN on today's `main` **and on this
head** —

```
$ gh run list -R opencharly/plugin-check --workflow candy --limit 3
34711941344  feat/remove-dead-allowances  success  2026-09-12T18:41:02Z   <- this PR's head bf5091a
34708519768  main                        success  2026-09-12T17:32:15Z
34707639928  feat/steps-file-authoring   success  2026-09-12T17:14:41Z
```

It is an **org-wide stamp lag**, verified across all six sibling plugin repos (`plugin-build`,
`plugin-adb`, `plugin-docs`, `plugin-box`, `plugin-clean`, `plugin-loader` — every root manifest
`version: 2026.232.0520`).

**Named batch: `schema-stamp-lag`** — owner `opencharly/charly` + the umbrella sync; scope = the
org-wide `charly migrate` stamp bump on the repo root manifests **plus** the per-repo
`.github/workflows/deploy.yml` charly-pin bump, which MUST ride together (bumping the stamp in this repo
alone to the dev floor `2026.248.1030` exceeds the pinned CI charly and would red the candy CI) — which is
exactly why that is the batch's unit of work and not this dead-weight PR's. Reported to the maintainer in
this session; no in-tree change is made here.


## Rulebook compliance

Every rule below carries an applicability decision; `N/A` names why the change class excludes it.

* **R0 — skills first.** `/charly-internals:git-workflow` and `/charly-internals:strict-policy` were
  loaded before the first tool call: the dispatcher rows for git/PR-only landing and for the
  failure-discipline trigger both matched this task.
* **R1 — RCA every anomaly.** Three anomalies surfaced and each carries an RCA + disposition: (a) the
  `charly box validate` exit-1 (RCA and named batch above); (b) the `systemd-unit-file-daemon-reload`
  matcher gap (RCA + counts in section 3 — the only remedy is a gate-loosening pattern widening, a
  maintainer decision, so it is reported rather than taken); (c) the config header's now-false
  "a deferred Close … is idiomatic best-effort cleanup" clause — FIXED in this diff, since the removal
  made it untrue.
* **R2 — finish the whole cutover.** Blocking findings are fixed in this tree; the ONE non-blocking
  find is routed to a NAMED batch (`schema-stamp-lag`, owner + scope + the reason the stamp and the CI
  pin must ride together), and the plugin-build residue is named with file:line + owner. No
  "pre-existing" / "out of scope" / "follow-up PR" classification is used.
* **R3 — no duplication.** The removed exclusion duplicated what the per-site handling already does;
  no canonical behaviour is re-implemented, and the three `fmt.Fprint*` exclusions stay because they
  are the CLI idiom's ONE home.
* **R4 — no workarounds.** No sleeps, retries, magic numbers, environment shims or manual
  podman/virsh/systemctl calls: the `go.sum` change is `go mod tidy`'s own output (not a hand-edit) and
  the exclusion deletion is verified by the linter itself.
* **R4a — fix the product first; documentation never routes around a defect.** The diff touches prose
  (the `.golangci.yml` header), so the header was re-derived against reality rather than left standing:
  the removal made its Close clause false, and it now records the deliberate divergence from charly's
  config plus the per-site handling. No documented workaround is added and no failing command is papered
  over.
* **R5 — delete legacy completely.** The removed items leave no functional residue; the claim-keyed
  sweep is pasted verbatim (below).
* **R6 — git safety.** `git status` clean before each commit; `feat/` branch, PR-only landing, no
  force-push, no pushed-history rewrite, no hook bypass, no direct push to `main`.
* **R7 — prove behavior, not compilation.** The change class is tooling config + module metadata, with
  no runtime path: the behaviour to prove is the linter's own verdict under the edited config, executed
  live (`golangci-lint run ./...` = `0 issues.`) and paired with the reversal proof that the remaining
  exclusions are load-bearing. No bed exercises a lint config, and the candy's runtime surface is
  untouched — `charly box build` / `charly check box` / `check live` assert none of this diff.
* **R8 — verify emitted artifacts.** N/A — this diff emits no generated artifact: it changes no
  `plan:` step, no Containerfile input and no `ai.opencharly.*` label (the candy manifest is untouched),
  so there is nothing for `charly box labels` to re-assert.
* **R9 — binary equals source.** N/A — no binary is built or invoked by this change; the module
  metadata edit is proven by the Go toolchain itself (`go build`, `go vet`, `go test`, `go mod
  tidy -diff`) against the committed tree.
* **R10 — fresh disposable proof.** The full gate ran on the committed tree and the changed artifact
  was executed live with the removal in place; the body-only commits that follow carry NO diff, so the
  proven tree is byte-identical (pasted in *How tested*).

**R5 sweep, verbatim (executed on the committed tree):**

```
$ git grep -n '(io.Closer).Close' -- candy/plugin-check/
candy/plugin-check/.golangci.yml:3:# config's (io.Closer).Close exclusion — see the last paragraph for why.
candy/plugin-check/.golangci.yml:9:# neither is (io.Closer).Close: every Close in this module is handled explicitly at its own site (a
$ echo $?
0

$ git grep -n 'v0.2026252.834\|v0.2026253.825' -- candy/plugin-check/
candy/plugin-check/plugin_runner.go:42:// (the sdk v0.2026252.834 cutover) - the do:act state-provision verbs
$ echo $?
0
```

Both surviving mentions are deliberate and named: the two `.golangci.yml` lines are the comment this PR
adds to record WHY the exclusion is not carried over (no `exclude-functions` entry for it remains), and
`plugin_runner.go:42` is a historical provenance note naming the sdk cutover that introduced those
verbs — not a claim about the current pin. No `go.sum` line and no config entry survives.
## Change classification

* Change class: **tooling config + module metadata** (no runtime code).
* Verification gate: `gofmt` / `go build` / `go vet` / `go test` / `golangci-lint` + the evidence
  commands, all pasted above.
* Attribution tier: **fully tested and validated** — the full gate named above passed on the final tree
  and the changed artifact was executed live.

*Assisted-by: pi ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*