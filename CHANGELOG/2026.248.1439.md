# 2026.248.1439 — instrument: pipeline stages accept the desugared plugin/plugin_input pair

## Summary

parsePipelines treated every key of a desugared pipeline stage as a verb, so the authored `transcode: {}` pipeline (desugared to plugin/plugin_input) was rejected as two verbs. The fix mirrors instrumentVerb: the plugin/plugin_input pair is ONE verb. Surfaced by the wave's check-instrument-omarchy-vm run; this push adds the B12 unit tests.

## How tested (pasted + LIVE)

```
$ cd plugin-check/candy/plugin-check && go test -count=1 -run "TestParsePipelines|TestInstrument" -v .
=== RUN   TestInstrumentBracketDispatch
--- PASS: TestInstrumentBracketDispatch (0.00s)
=== RUN   TestInstrumentBracketPhaseGating
--- PASS: TestInstrumentBracketPhaseGating (0.00s)
=== RUN   TestParsePipelinesDesugaredPair
--- PASS: TestParsePipelinesDesugaredPair (0.00s)
=== RUN   TestParsePipelinesTwoSugarVerbsRejected
--- PASS: TestParsePipelinesTwoSugarVerbsRejected (0.00s)
```

**LIVE (the changed path executed on a fresh rebuild):** the wave's check-instrument-omarchy-vm run used the FIXED binary - the instrument resolution (which calls parsePipelines) proceeded past the pre-fix rejection to vm-build/vm-create/deploy-add/check-live PASS + instrument starts PASS (the pre-fix run failed exactly at the pipeline rejection).

## Rulebook compliance

R1 (surfaced by the live bed run, fixed + unit-gated), R3 (one desugar contract, mirrors instrumentVerb), boundary law, R6 (PR-only).

## Change classification

Class: code change. Gate: unit tests + the live omarchy run (the changed path executed). Attribution: analysed on a live system (fresh unit gates + the changed path ran live in the wave bed).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*