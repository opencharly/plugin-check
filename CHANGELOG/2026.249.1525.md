# 2026.249.1525 — feat(check): the live VM target hops the deploy chain (Phase 3)

## Summary

The **check-live VM target hops the deploy chain** (Phase 3): `pluginResolveVmTarget` derived the vmName directly from the deploy's from: — for the converted beds the from: names the clone-base BED (a deploy whose own from: names the terminal template), so the live spec lookup missed → the nil spec panicked the shared port resolver. The target now hops the chain ONCE (an entry.From that is itself a bed resolves to THE BED's from:; an entry.From NOT in the tree IS the template already — no hop): the same single-hop semantics as sdk/loaderkit DeployTargetEntity.

## How tested (pasted, LIVE)

```
$ go test ./... -run TestPluginResolveVmTarget -v
--- PASS: TestPluginResolveVmTarget_LeafVmUnderNonVmParent
--- PASS: TestPluginResolveVmTarget_DeployHop   # bed-from-bed hops; bed-from-template passes through (fails without the hop)
ok  github.com/opencharly/plugin-check/candy/plugin-check
$ go build ./... && go vet ./...

$ charly check run check-instrument-cachyos-vm   # target: disposable: true (check-instrument-cachyos-vm.yml:63)
PASS (steps=9) — exit=0, 113s — errors 0, warnings 0, allowlisted 31, UN-allowlisted 0
  vm-build 1s | vm-create 1s | deploy-add 49s | check-live 7s | update 49s | cleanup 2s
  evidence: frames.mp4 (h264 1280x800, 49 frames) + .cast (asciinema v3)
  (run on the FRESH worktree rebuild — the binary was rebuilt after every wave fix)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: the panic stack traced to the missing hop + the nil spec; the R10 run pasted.
- R2: same named batch (Phase 3); the nil-guards ride sdk #220.
- R3: the hop matches the ONE chain resolver (sdk/loaderkit DeployTargetEntity).
- R4: no workarounds; the dead always-true guard + its helper deleted (R5).
- R10: the hop is unit-gated (fails without it) + the live disposable R10 run passed.

## Change classification

Class: live-target resolution (functional hop). Gate: the unit gate + the live disposable R10 run. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*