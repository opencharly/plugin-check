# 2026.245.1503 — feat(check): recordings.yml manifest (G5) - recording evidence discoverable + teardown survival asserted

## Summary

Adds the **recordings manifest (G5)** to the disposable bed runner: after a bed run, `writeRecordingsManifest` scans the live-phase step logs for the record-verb stop outputs (plugin-record "Recording stopped (mode: …); saved N bytes to <path>" and plugin-spice "Recording stopped (name: …, frames: N, bytes: N)"), stats each artifact on the HOST post-teardown, and writes `.check/<bed>/<calver>/recordings.yml` next to summary.yml. A present artifact at write time IS the copy-before-teardown survival assertion: the recording was pulled host-side during the live phases and survived the venue destroy. The full bed-level whole-run wrap (a deploy `record:` field starting a recording around the live phases) is routed to the named **"whole-run recording wrap" follow-up batch** (spec-schema + runner-injection surface; spec = plan/screenrecording-support.md §14; it will be its own PR in that batch).

**Changes:** recordings.go (scanRecordingsLog parser + writeRecordingsManifest host-stat/YAML emitter), the runner hook at the final summary write, unit tests (TestScanRecordingsLog/Empty, TestAtoi). Deterministic parser fully covered.

## How tested

```
$ cd candy/plugin-check && go build ./... && go vet ./... && go test -run 'Recordings|Atoi' ./...
ok  github.com/opencharly/plugin-check/candy/plugin-check  0.004s
```

**R10 fresh-rebuild proof (disposable omarchy VM bed, calver 2026.245.1445) — EXIT=0 PASS (steps=9):** check-live PASS 26.6s → update PASS 5m01s → check-live-rebuild PASS 20.7s → cleanup PASS 3m02s (vm destroyed; virsh count 0). The manifest landed at `.check/check-spike-omarchy-vm-mp4/2026.245.1445/recordings.yml` next to summary.yml, verbatim:

```yaml
recordings:
  - name: spike-omarchy-vm.mp4
    mode: desktop
    bytes: 753905
    artifact: /tmp/spike-omarchy-vm.mp4
    survived_teardown: true
  - name: spike-omarchy-vm.mp4
    mode: desktop
    bytes: 748779
    artifact: /tmp/spike-omarchy-vm.mp4
    survived_teardown: true
```

Both live-phase captures (check-live + check-live-rebuild) are recorded with the artifact paths and `survived_teardown: true` — written AFTER the cleanup step destroyed the VM, so the survival flag is a structural assertion, not a promise.

## Rulebook compliance

- R1: change via PR to the owning repo (plugin-check); branch `feat/bed-record-wrap` off main — HOW: PR flow, this PR.
- R2: the whole-run wrap deferral names its batch ("whole-run recording wrap" follow-up; plan §14) — HOW: body §6.
- R3: one manifest implementation; no duplication with summary.yml (steps/diagnostics vs recordings) — HOW: separate writer + file.
- R4: no workarounds/sleeps — the post-teardown write is the survival assertion by design, not a bypass — HOW: writeRecordingsManifest runs at the final summary.
- R5: no legacy deleted (new additive surface) — N/A — <reason: no replaced behavior in this change>.
- R6: git safety — no force-push/direct-main; PR-only — HOW: committed + pushed to the feature branch only.
- R8: no silent config drift — the manifest is a new artifact in an existing convention (.check/<bed>/<calver>/) — HOW: mirrors summary.yml placement.
- R9: no cross-repo hardcoding — paths come from the run's own logs — HOW: scanRecordingsLog reads only the bed's step logs.
- R10: fresh disposable proof from the committed tree — HOW: the R10 bed ran with a locally-built binary from the exact branch tree (go.mod replace disclosed; stamped binary 2026.245.1311; EXIT=0 9/9; recordings.yml survived teardown).
- RDD: the manifest is derived from the run's recorded evidence, not a claim — HOW: log-scan + host stat.
- ADE: acceptance is the R10 bed's verdicts, not a promise — HOW: per-check PASS matrix above.
- SDD: schema-consistent — the manifest is YAML alongside summary.yml; no spec change in this PR (the wrap's spec field is the named batch) — HOW: recordings.go + tests.

## Change classification

- **Change class:** feature (bed-runner recordings manifest, G5)
- **Verification gate:** unit tests green + R10 fresh-rebuild bed green (EXIT=0 9/9) with the manifest + survival asserted
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*